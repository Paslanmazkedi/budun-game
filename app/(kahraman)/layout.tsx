import CharacterHubLayout from '@/components/CharacterHubLayout'

export default function KahramanLayout({ children }: { children: React.ReactNode }) {
  return <CharacterHubLayout>{children}</CharacterHubLayout>
}
